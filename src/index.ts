import { LightningMessageboard } from './lightning-messageboard';

// Export the component class
export { LightningMessageboard };

// Register the web component
customElements.define('lightning-messageboard', LightningMessageboard);
